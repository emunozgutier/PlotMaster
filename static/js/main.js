import TouchPlot from './TouchPlot.js';

document.addEventListener('DOMContentLoaded', () => {
    // Example usage
    var touchPlot = new TouchPlot();

    // Make myTouchPlot globally accessible
    window.touchPlot = touchPlot;
});